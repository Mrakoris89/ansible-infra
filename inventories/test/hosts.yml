all:
  children:
    dockers:
      hosts:
        debian.test.lab:
        fedora.test.lab:
        centos7.test.lab:
        centos9.test.lab:
        ubuntu.test.lab:
    kvm:
      hosts:
        kvm1.test.lab:
    gateway:
      hosts:
        gw.test.lab:
